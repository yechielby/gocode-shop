{
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.12.13",
    "nodemon": "^2.0.7"
  },
  "scripts": {
    "start": "node server.js",
    "start-dev": "nodemon server.js",
    "dev": "concurrently \"nodemon server.js\" \"cd client && npm start\"",
    "heroku-postbuild": "cd client && npm i -f && npm run build"
  },
  "devDependencies": {
    "concurrently": "^6.2.0"
  }
}
